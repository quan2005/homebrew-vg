# homebrew-vg

Homebrew tap for `vg`.

## Install

```bash
brew tap quan2005/vg
brew install vg
```

## Upgrade

```bash
brew upgrade vg
```

This tap is updated automatically by the `quan2005/vector-grep` release workflow.
